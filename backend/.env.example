PORT=5050
GEMINI_API_KEY=your_key_here
# Optional: comma-separated fallback order if one model is overloaded.
GEMINI_MODEL_CANDIDATES=gemini-3-flash-preview,gemini-2.5-flash,gemini-2.5-flash-lite
# Optional: retries per model for transient errors (503/429/5xx).
GEMINI_RETRIES_PER_MODEL=2
# Optional: exponential backoff tuning (milliseconds).
GEMINI_RETRY_BASE_MS=900
GEMINI_RETRY_MAX_MS=6000
PGHOST=localhost
PGPORT=5432
PGUSER=admin
PGPASSWORD=admin
PGDATABASE=admin
SENSOR_TABLE=sensor_data
HISTORICAL_TABLE=historical_data
ROLLUP_ENABLED=true
FORECAST_MODEL_PATH=../model-training/artefacts/XGB/xgb_3models_robust.joblib
FORECAST_SCRIPT_PATH=./scripts/forecast_xgb.py
FORECAST_PYTHON_BIN=python3
DATAGOV_SCRIPT_PATH=./scripts/live_datagov_fetch.py
DATAGOV_PYTHON_BIN=python3
DATAGOV_STATION_ID=S109
# Polling is fixed at 5 minutes in backend code; this value is informational.
DATAGOV_POLL_MS=300000
DATAGOV_TIMEOUT_SECONDS=8
DATAGOV_HISTORY_HOURS=24
# Optional: include only if your data.gov account requires authenticated quota.
DATAGOV_API_KEY=
